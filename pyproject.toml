[project]
name = "flask-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "surtr", email = "pudi_258@qq.com" }
]
requires-python = ">=3.11"
dependencies = [
    "flask-sqlalchemy>=3.0",
    "python-dotenv>=1.2.3",
    "sqlalchemy>=2.0",
]

[project.scripts]
flask-demo = "flask_demo:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
